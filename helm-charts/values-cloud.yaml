# Configuration de base pour TOUS les environnements cloud
global:
  isCloud: true
  isLocal: false
  protocol: https

# Images cloud avec tags stables
images:
  textGeneration:
    tag: v1.0.0
  imageProcessing:
    tag: v1.0.0

# Resources cloud appropriées
services:
  text-generation:
    replicaCount: 2
    resources:
      requests:
        memory: "512Mi"
        cpu: "250m"
      limits:
        memory: "1Gi"
        cpu: "500m"

# Ingress activé pour cloud
ingress:
  enabled: true

# Monitoring activé pour cloud
monitoring:
  enabled: true
